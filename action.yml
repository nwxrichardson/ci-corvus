name: 'corvus-comment'
description: 'Generate a comment showing Corvus comparisons'
branding:
  icon: 'box'
  color: 'purple'
inputs: 
  aird-location:
    default: ''
runs:
  using: 'composite'
  steps:
    - name: Checkout ci-corvus
      uses: actions/checkout@v4
      with: 
        repository: nwxrichardson/ci-corvus
    - name: Checkout ci-corvus
      uses: actions/checkout@v4
      with: 
        repository: epsilonlabs/sirius-pr-action-workspace
        path: model/new
    - name: Checkout ci-corvus
      uses: actions/checkout@v4
      with: 
        repository: epsilonlabs/sirius-pr-action-workspace
        path: model/old
        ref: main
    - name: Docker
      run : docker build -t ci-corvus:latest . 
      shell: bash
    - name: Docker run
      run : docker run -v $(pwd)/output:/output/ -v $(pwd)/model/new/${{ github.event.inputs.aird-location }}:/example/new -v $(pwd)/model/old/${{ github.event.inputs.aird-location }}:/example/old ci-corvus:latest
      shell: bash
    - name: Upload README to S3
      run: |
          aws s3 cp --recursive $(pwd)/output/model/ s3://${{ env.BUCKET_NAME }}/model
      shell: bash  
    - name: Create comment from markdown file
      uses: GrantBirki/comment@v2.1.1
      with:
        file: output/plain-sample.md
